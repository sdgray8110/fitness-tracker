<ul id="monthly-overview-tabs" class="tabs clearfix">
{{#tabs}}
    <li>
        <button {{#className}}class="{{.}}"{{/className}} type="button" data-path="{{year}}/{{month}}">{{name}}</button>
    </li>
{{/tabs}}

    <li class="year-selection pull-right">
        {{> rides/yearSelection}}
    </li>
</ul>